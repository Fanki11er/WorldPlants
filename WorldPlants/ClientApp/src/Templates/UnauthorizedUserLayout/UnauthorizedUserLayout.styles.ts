import styled from "styled-components";
import { AppTheme } from "../../GlobalStyles/theme";

export const UnauthorizedUserLayoutWrapper = styled.div`
  display: grid;
  grid-template-rows: auto;
  grid-template-columns: 1fr;

`;
